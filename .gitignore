# Dependencies (huge, regenerated by "npm install", never committed)
node_modules

# Build output
dist

# Local env / secrets (API keys go here later, never in git)
.env
.env.local

# Editor / OS noise
.DS_Store
*.log
